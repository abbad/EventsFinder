<Page
    x:Name="MapPage"
    x:Class="Event_Finder.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Event_Finder.Views"
    xmlns:viewModel="using:Event_Finder.ViewModel"
    xmlns:model="using:Event_Finder.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:bm="using:Bing.Maps"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="toolTipStyle" TargetType="ToolTip">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border CornerRadius="5">
                                <Border.Background>
                                    <SolidColorBrush Color="Black" Opacity="0.5"/>
                                </Border.Background>
                                <ContentPresenter Margin="5">
                                </ContentPresenter>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <model:Event x:Key="Event" />

            <ControlTemplate x:Key="PushPinStyle">
                <Grid Width="200" Height="200">
                    <Image Source="{Binding pic_big}" Width="200" Height="200"/>
                </Grid>
            </ControlTemplate>

            <DataTemplate x:Key="LogoTemplate">
                <bm:Pushpin  bm:MapLayer.Position="{Binding Location}" Template="{StaticResource PushPinStyle}" PointerPressed="Pushpin_PointerPressed" >
                    <ToolTipService.ToolTip >
                        <ToolTip Style="{StaticResource toolTipStyle}">
                            <StackPanel Margin="5" MaxWidth="200">
                                <TextBlock Text="{Binding name}" FontWeight="Bold" FontSize="16" Foreground="White"/>
                                <TextBlock Text="{Binding description}" Foreground="White" TextWrapping="Wrap"/>
                            </StackPanel>
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </bm:Pushpin>
            </DataTemplate>
        </ResourceDictionary>
    </Page.Resources>


    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="72"/>
            <RowDefinition Height="72"/>
            <RowDefinition Height="624" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1076"/>
            <ColumnDefinition Width="290"/>
        </Grid.ColumnDefinitions>


        <!-- row 0 logo, name, search, toggle buttons -->
        <TextBlock Text="EVENT FINDER" Grid.Row="0" FontSize="50" VerticalAlignment="Center" HorizontalAlignment="Left" Height="60" Margin="0,6" Width="324"  />

        <!-- row 1 Categories, Event labels  -->
        <TextBlock Grid.Row="1" Grid.Column="1" Text="Events" HorizontalAlignment="Center" VerticalAlignment="Center"  FontSize="40" FontFamily="Leelawadee UI" Margin="58,10,44,9" Height="53" Width="188"/>

        <!-- row 2 Categories, Events stacks -->

        <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <ListView IsItemClickEnabled="True" ItemClick="ListView_ItemClick" Height="619" SelectionMode="None" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ItemsSource="{Binding pushpinCollection}" IsSwipeEnabled="False" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="10">
                            <TextBlock Foreground="Azure" TextWrapping="Wrap">
                    			<Bold>
                    				<Run Text="Event:"/>
                    			</Bold>
                    			<Run Text="{Binding name}"/>
                            </TextBlock>
                            <TextBlock Foreground="Azure" TextWrapping="Wrap">
                    			<Bold>
                    				<Run Text="Date:"/>
                    			</Bold>
                    			<Run Text="{Binding start_time}" />
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>


        </StackPanel>

        <!-- row 3 Date, Legend labels -->

        <!-- row 4 Date, Legends Stack -->
        <Border Grid.Row="2" Grid.Column="0" Background="#FF1551C9" VerticalAlignment="Center" HorizontalAlignment="Center" Height="0" Margin="100,537,976,87" Width="0"/>

        <bm:Map  Credentials="AqzQTQg1GrHIoL2a5Ycf08czzcxAooMpXiADdOgZQYPBtwpuSSf8Fd4y7MUTJo-h" ShowNavigationBar="False"  x:Name="MainMap" Loaded="OnLoad" Grid.Row="2">
            
            <ProgressRing x:Name="prog" Height="200" Width="200"  IsActive="True" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <bm:MapLayer>
                <Grid x:Name="Infobox" Visibility="Collapsed" Margin="0,-115,-15,0">
                    <Border Width="300" Height="110" Background="Black" Opacity="0.8" BorderBrush="White" BorderThickness="2" CornerRadius="5"/>

                    <StackPanel Height="100" Margin="5">
                        <Grid Height="40">
                            <TextBlock Text="{Binding name}" FontSize="20" Width="250" TextWrapping="Wrap" HorizontalAlignment="Left" />
                            <Button Content="X" Tapped="CloseInfobox_Tapped" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                        </Grid>
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" MaxHeight="60">
                            <TextBlock Text="{Binding description}" FontSize="16" Width="290" TextWrapping="Wrap" Height="Auto"/>
                        </ScrollViewer>
                    </StackPanel>
                </Grid>
            </bm:MapLayer>


            <bm:MapItemsControl x:Name="ListOfpushpins"  ItemTemplate="{StaticResource LogoTemplate}" ItemsSource="{Binding pushpinCollection}" >
            
            
            </bm:MapItemsControl>
            <!--<bm:MapItemsControl ItemTemplate="{StaticResource PinTemplate}" ItemsSource="{Binding pushpincoll}" Height="100" Width="100"/>-->
            <!--<bm:Pushpin Name="myPin" Height="100" Width="100"/>-->
            <!-- Tapped="pushPin_Tapped" n76o fog 3nd mapItems Control-->
            <!-- <Popup VerticalOffset="200"  HorizontalOffset="300" x:Name="Image" IsLightDismissEnabled="True" Height="2000" Width="2000" >
                <Image Name="myImage" Height="300" Width="300" Source="Assets/Logo.scale-100.png"/>
            </Popup>-->
        </bm:Map>
        <DatePicker x:Name="endRangeDateTimePicker" DayFormat="{}{day.integer} {dayofweek.full}" HorizontalAlignment="Center" DateChanged="DatePicker_DateChanged" Margin="680,18,20,22" Height="32" Width="376" Grid.Row="1" />
        <DatePicker x:Name="startRangeDateTimePicker"  DayFormat="{}{day.integer} {dayofweek.full}" HorizontalAlignment="Center" DateChanged="DatePicker_DateChanged" Margin="47,18,653,22" Height="32" Width="376" Grid.Row="1" />
        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="40" Margin="436,10,422,14" Height="48" Width="218" RenderTransformOrigin="-0.699,0.51" Grid.Row="1" >
        	<Run Text="Date Range"/>
        	<LineBreak/>
        	<Run/>
        </TextBlock>
    </Grid>

</Page>
